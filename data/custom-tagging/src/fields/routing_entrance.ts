import type { CustomField } from '../types';

/** `routing:entrance` vertex tag (Quebec routing network entrances). */
export const routingEntrance: CustomField = {
    key: 'routing:entrance',
    type: 'typeCombo',
    label: 'Type'
};
